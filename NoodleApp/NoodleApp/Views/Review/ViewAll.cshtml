@model List<NoodleApp.Models.Review>
@{
    Layout = "_Layout";
}

<div class="container">

    <h2 id="reviewTitle">All Reviews for Noodles </h2>

    @foreach (var x in Model)
    {
        <table>
            <tr>
                <td>Review: @x.Name</td>
            </tr>
        </table>
        <p><a asp-controller="Review" asp-action="Update" asp-route-id="@x.ID">Update Review</a></p>
    }
</div>